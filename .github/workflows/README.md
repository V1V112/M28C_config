# 自动取消跟踪已忽略文件

`.gitignore` 默认不会取消 Git 对已有文件的跟踪。修改并推送任意
`.gitignore` 文件后，`sync-gitignore.yml` 会创建提交，从仓库当前版本中删除
新匹配忽略规则的已跟踪文件，但不会删除 Git 历史。

工作流执行 `git rm --cached`，不会直接删除运行器工作区中的文件。不过，云端
生成删除提交后，本地拉取该提交可能删除原先被跟踪的本地文件。首次使用前，
请备份重要文件。
